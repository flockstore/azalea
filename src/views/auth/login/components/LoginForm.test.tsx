describe("LoginForm Component", () => {

    it("should render without crashing", () => {

    });

    it("should receive the correct values from form", () => {

    });

    it("should show validation errors when present", () => {

    });

    it("should set loading after form submit and remove loading after after finally block", () => {

    });

    it("should call sign in when form is sent correctly, send notification and mark state as sent", () => {

    });

    it("should log error and show error notification when signIn error is present", () => {

    });

    it("should set 30s interval after sent and clear resend after interval executed", () => {

    });

    it("should display correct badge matching interval and hide when not present", () => {

    });

});