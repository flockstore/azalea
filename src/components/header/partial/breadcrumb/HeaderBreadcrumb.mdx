import {Canvas, Controls, Meta} from "@storybook/blocks";

import * as HeaderBreadcrumb from "./HeaderBreadcrumb.stories";

<Meta of={HeaderBreadcrumb} />

# Header Breadcrumb

The header breadcrumb is a component showing the actual state
of deep navigation to the user, allowing them to get back in
all the navigation tree.

<Controls/>

## Default

<Canvas of={HeaderBreadcrumb.Default}/>