import {Canvas, Controls, Meta} from "@storybook/blocks";

import * as SidebarShrink from "./SidebarShrink.stories";

<Meta of={SidebarShrink} />

# Sidebar Shrink

The sidebar shrink/expand button is a graphical representation which
is in charge of updating the sidebar state whenever the user clicks it.

<Controls/>

## Example of default shrink

<Canvas of={SidebarShrink.Default}/>