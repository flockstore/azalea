import {Canvas, Controls, Meta} from "@storybook/blocks";

import * as SidebarSwitcher from "./SidebarSwitcher.stories";

<Meta of={SidebarSwitcher} />

# Sidebar Switcher

The sidebar switcher is a component in charge of providing
the user ability to change quickly between dark and light mode.

<Controls/>

## Example of default control.

<Canvas of={SidebarSwitcher.Default}/>