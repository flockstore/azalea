import {Canvas, Controls, Meta} from "@storybook/blocks";

import * as DropNavigation from "./DropNavigation.stories";

<Meta of={DropNavigation} />

# Drop Navigation

The Drop Navigation is a conditional manageable dropdown menu,
which can be a collapsable or an always-expanded navigation
menu filled with links.

<Controls/>

## Example of default navigation

<Canvas of={DropNavigation.Default} />